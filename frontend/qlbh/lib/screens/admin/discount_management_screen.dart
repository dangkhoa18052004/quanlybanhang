// lib/screens/admin/discount_management_screen.dart
import 'package:flutter/material.dart';

class DiscountManagementScreen extends StatelessWidget {
  const DiscountManagementScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Quản lý Mã giảm giá')),
      body: const Center(child: Text('Discount Management - Coming Soon')),
    );
  }
}
